apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: readinglist
spec:
  replicas: 3
  selector:
    matchLabels:
      app: readinglist
  template:
    metadata:
      labels:
        app: readinglist
    spec:
      containers:
      - name: readinglist
        image: crojasmatas/readinglist:0.2
        livenessProbe:
          httpGet:
            port: 8080
            path: /health
          initialDelaySeconds: 10

